import { useState } from "react";
import Header from "@components/header";

function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      <h1>App</h1>
    </>
  );
}

export default App;
